@model List<JsonCarouselItem>
@{if (Model != null)
  {
      if (Model.Count() > 0)
      {
          var item = Model.First();
    <div id="background_no_cycler">
        <img class="active" src="@item.BannerImageUrl" alt="@item.Name" />
    </div>
      }
  }             
}
