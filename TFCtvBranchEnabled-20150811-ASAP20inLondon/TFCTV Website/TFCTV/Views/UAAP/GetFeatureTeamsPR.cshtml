@model List<TFCTV.Helpers.JsonFeatureItem>
@{
    if (Model != null)
    {
        if (Model.Count() > 0)
        {
            foreach (var item in Model)
            { 
                
    <text>
    <p>
        @Html.ActionLink(item.CelebrityFullName, "TeamDetails", new { id = item.ShowId }, null)
    </p>
    </text>
            }
        }
    }   
}
