@{
    
    string creditsElementId = "credit-balance";
}
<div id="usercredits" class="floatLeft">
    <ul>
        <li class="padLeftSmall"><a class="user-credits"
            id="@creditsElementId" href="#" rel="#overlay">Reload Credits @ViewBag.Balance</a></li></ul>
</div>
@*<div class="apple_overlay" id="reloadOL">
    <div class="contentWrap">
    </div>
</div>*@ @*<script type="text/javascript">
    $(document).ready(function () {
        $('#credit-balance').overlay({
            mask: 'black',
            fixed: false,
            closeOnClick: false,
            onBeforeLoad: function () {
                $("#overlay").width(640);
                $('#overlay .contentWrap').html('');
                var wrap = this.getOverlay().find(".contentWrap");
                wrap.load(this.getTrigger().attr("href"));
            },
            onClose: function () {
                $('#overlay .contentWrap').empty();
            }
        });
    });
</script>*@
@Html.Action("Balance", "Load", new { CreditsElementId = creditsElementId })
